# outputs/
> L2 | 父级: /CLAUDE.md

## 成员清单

CLAUDE.md: outputs 目录地图，说明这里是 renderer 的本地输出目录。

## 架构边界

这里保存本地生成物，不放源模板和判断规则。PNG 可复生成时，以 `assets/`、`scripts/` 和输入 JSON 为真相源；`outputs/*.png` 被 `.gitignore` 排除，不进入 skill 包。

[PROTOCOL]: 变更时更新此头部，然后检查 CLAUDE.md
