# agents/
> L2 | 父级: /CLAUDE.md

## 成员清单

openai.yaml: Codex UI 元数据，定义显示名、短描述、默认提示和隐式触发策略。
CLAUDE.md: agents 目录地图，避免 UI 元数据与 skill 逻辑混杂。

## 架构边界

这里不放人格规则。UI 元数据只描述如何展示和触发 skill，不能改变输出逻辑。

[PROTOCOL]: 变更时更新此头部，然后检查 CLAUDE.md
